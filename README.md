# ANN
Build and train a simple Artificial Neural Network (ANN) in TensorFlow/Keras for binary classification. The project includes data preprocessing, train/test splitting, feature scaling, a Dense(16) → Dense(8) → Dense(1) architecture, Adam optimization, binary cross-entropy loss, model evaluation, and training vs. validation loss visualization.
